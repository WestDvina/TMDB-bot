[project]
name = "tmdb-bot"
version = "1.0.0"
description = "Telegram bot for searching movies, series and people via TMDB"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.4",
    "aiohttp>=3.9",
    "python-dotenv>=1.0",
]
